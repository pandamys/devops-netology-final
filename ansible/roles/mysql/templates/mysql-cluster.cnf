[ndbd default]
NoOfReplicas=2

[ndb_mgmd]
hostname={{ management_address }}
datadir={{ mysql_data }}

[ndbd]
NodeId=1
hostname={{ node1_address }}
datadir={{ mysql_data }}

[ndbd]
NodeId=2
hostname={{ node2_address }}
datadir={{ mysql_data }}

[mysqld]
# SQL node options:
hostname={{ management_address }}