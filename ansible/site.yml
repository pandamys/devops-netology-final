---
    - name: Install nginx
      hosts: webservers
      become: yes
      roles:
        - nginx

    - name: "Install mysql"
      hosts: databases
      become: yes
      roles:
        - mysql

    - name: "Install wordpress"
      hosts: wordpress
      become: yes
      roles:
        - wordpress